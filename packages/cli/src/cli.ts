// Stub entry point. The real implementation is moved into place by
// worker 20 in the step immediately after this scaffold lands. Until
// then, this file exists so that `@mux-magic/cli` is a real workspace
// package whose `--help` smoke test can fail visibly (no yargs wiring,
// no command list) and then go green once the wiring is moved over.
console.log("Mux-Magic CLI: scaffold (no commands wired yet)")
