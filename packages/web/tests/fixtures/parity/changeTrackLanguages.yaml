paths:
  basePath:
    label: Base Path
    value: /fixture/media
steps:
  - id: step-fixture
    command: changeTrackLanguages
    params: {sourcePath: '@basePath', isRecursive: true, audioLanguage: eng, subtitlesLanguage: eng, videoLanguage: jpn}
