paths:
  basePath:
    label: Base Path
    value: /fixture/media
steps:
  - id: step-fixture
    command: keepLanguages
    params: {sourcePath: '@basePath', isRecursive: true, audioLanguages: [eng, jpn], subtitlesLanguages: [eng], useFirstAudioLanguage: true, useFirstSubtitlesLanguage: true}
