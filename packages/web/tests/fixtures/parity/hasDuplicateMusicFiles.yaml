paths:
  basePath:
    label: Base Path
    value: /fixture/media
steps:
  - id: step-fixture
    command: hasDuplicateMusicFiles
    params: {sourcePath: '@basePath', isRecursive: true, recursiveDepth: 2}
