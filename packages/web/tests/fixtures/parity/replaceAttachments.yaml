paths:
  basePath:
    label: Base Path
    value: /fixture/media
steps:
  - id: step-fixture
    command: replaceAttachments
    params: {sourceFilesPath: '@basePath', destinationFilesPath: '@basePath'}
