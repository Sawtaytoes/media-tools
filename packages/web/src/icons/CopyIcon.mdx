{/* CopyIcon.mdx */}

import { Meta, Canvas } from "@storybook/blocks";
import * as CopyIconStories from "./CopyIcon.stories";

<Meta of={CopyIconStories} />

# CopyIcon

An inline SVG clipboard icon used on step and group card copy buttons. Inherits color from `currentColor` so it adapts to any text color applied by the parent.

## Default

<Canvas of={CopyIconStories.Default} />
