{/* CopyIcon.mdx */}

import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as CopyIconStories from "./CopyIcon.stories";

<Meta of={CopyIconStories} />

# CopyIcon

A 14×14px clipboard copy SVG icon. Used on step card headers and other copy actions. Inherits `currentColor` stroke so it respects the parent text color.

<Canvas of={CopyIconStories.Default} />
