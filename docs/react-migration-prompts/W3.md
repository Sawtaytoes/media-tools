# W3 spawn prompt — Final Cleanup

Paste the block below into a fresh Claude Code session opened in `d:\Projects\Personal\media-tools`.

---

You are Worker W3 in the React Migration Recovery for media-tools.

**Working directory:** `d:\Projects\Personal\media-tools`
**Branch:** `react-migration` (already checked out — do not switch)
**Your model:** Sonnet 4.6, **high effort** (originally scoped as medium, but the audit shows more production `window.mediaTools` usage than expected — bump up)
**Your role:** Delete all legacy JS. After you finish, the app is 100% React with zero `<script>` tags loading vanilla JS and zero `window.mediaTools` references.

W3 **blocks W4 + W5.** Until you finish, the app still depends on legacy code paths.

## Required reading before doing anything

1. [docs/react-migration-recovery-handout.md](../react-migration-recovery-handout.md) — Universal Rules (especially #4 — no snapshot/VRT tests; #8 — keep checklist honest).
2. [docs/react-migration-checklist.md](../react-migration-checklist.md) — current state. W2.5 just shipped DslRulesBuilder; Phase 2 is complete; you are next.
3. The W3 section of the handout — covers the deletion targets at a high level. This prompt supplements it with the **actual audit findings** (numbers below).

## Critical audit findings (from orchestrator scan)

**`window.mediaTools` usage in production code (only 3 files — the rest are tests/stories/MDX):**

- `packages/web/src/components/CommandHelpModal/CommandHelpModal.tsx` — **1 call**. Small.
- `packages/web/src/components/PathPicker/PathPicker.tsx` — **1 call** (`window.mediaTools?.setPathValue?.(`). Replace with a Jotai action — `pathsAtom` has setters; if a dedicated `setPathValueAtom` doesn't exist, port the mutation into `pathsAtom.ts` or `sequenceAtoms.ts` first.
- `packages/web/src/components/ApiRunModal/ApiRunModal.tsx` — **12 calls** (the heaviest). Read all 12 references first, group them by what they do, then design a small `apiRunModalAtoms.ts` or extend `useBuilderActions` to expose the needed actions. Replace in one commit per logical group; don't try to do all 12 in one mega-commit.

**`window.mediaTools` in tests/stories/MDX (19 files):**

These mock `window.mediaTools` to test the component as if the bridge still existed. Once you remove the production calls (above), the mocks become unused. Strategy:

- For test files (`*.test.tsx`): delete the mock setup; tests should still pass if the component now uses atoms directly.
- For stories (`*.stories.tsx`): delete the mock setup; story decorators (in `.storybook/preview.tsx`) already wrap stories in a Jotai `Provider` so atom-based components render correctly.
- For MDX (`*.mdx`): delete prose that describes "Selecting calls `window.setParam`" and replace with "Selecting dispatches to `setParamAtom` via `useBuilderActions`".

Files affected: CommandHelpModal (`.test.tsx`, `.stories.tsx`), ApiRunModal (`.mdx`), EnumPicker (`.mdx`, `.stories.tsx`, `.test.tsx`), CommandPicker (`.mdx`, `.stories.tsx`, `.test.tsx`), GroupCard (`.test.tsx`, `.stories.tsx`), PageHeader (`.mdx`), LoadModal (`.stories.tsx`), LookupModal (`.mdx`), PathPicker (`.mdx`, `.stories.tsx`), PathVarCard (`.test.tsx`), StepCard (`.mdx`, `.stories.tsx`).

**Legacy assets in `packages/web/public/` (delete all of these):**

```text
packages/web/public/
├── builder/                  # legacy builder bundle (~60 files); already not reached by /builder route in React
│   ├── builder.css
│   ├── index.html
│   └── js/                   # ~60 vanilla JS files
├── vendor/                   # tailwind-3.4.17.js, js-yaml.min.js, Sortable.min.js (all replaced by npm imports)
├── command-labels.js         # replaced by packages/web/src/jobs/commandLabels.ts (verify)
├── format-bandwidth.js       # replaced by packages/web/src/jobs/formatBandwidth.ts (verify)
├── format-bandwidth.test.ts  # tests for the above legacy JS; delete with it
├── index.html                # legacy jobs entry — DELETE (Vite root is packages/web/index.html)
├── jobs.css                  # replaced by Tailwind + builderStyles.css
├── progress-utils.css        # legacy CSS
├── progress-utils.js         # replaced by useSseStream/useTolerantEventSource (Wave F)
├── reload-on-restart.js      # dev-only reload helper; React HMR replaces it
├── sse-utils.js              # replaced by useSseStream (Wave F)
└── version-footer.js         # check if any React component renders a version footer
```

**`packages/web/src/types.window.d.ts`** — global type declaration for `window.mediaTools`. Delete it AFTER you've removed all `window.mediaTools` references. TypeScript will catch any stragglers.

**Vite entry is `packages/web/index.html`** (already minimal, just `<div id="root">` + `src/app.tsx`). You do NOT need to create a new entry — the legacy `public/index.html` was never the Vite entry; it was the legacy Hono-served fallback.

## Step-by-step (suggested order; you may resequence)

### Step 1 — Replace production `window.mediaTools` calls with atom dispatches

Recommended order (smallest blast radius first):

1. **CommandHelpModal.tsx** (1 call) — quick win, establishes pattern.
2. **PathPicker.tsx** (1 call) — needs `setPathValue` equivalent. Read `pathsAtom.ts` first; if no dedicated atom exists, add one. Use `useBuilderActions` style for the public API.
3. **ApiRunModal.tsx** (12 calls) — read all 12 first. Likely candidates: SSE stream callbacks, job-status mutations, modal-state transitions. Group into 2-3 logical commits.

For each commit:
- Run pre-push gate: `yarn test run && yarn typecheck && yarn lint`. **All three** must be green. Do not skip `yarn test run` — W2D shipped failing tests by skipping it; you must not repeat that.
- Manual smoke in dev server: `yarn dev`, exercise the affected component, verify no console errors.
- Commit message: `refactor(<component>): replace window.mediaTools with atom dispatch`.
- `git pull --rebase origin react-migration` before push (WBN-A is running on its own branch in `.claude/worktrees/wbn-a` — won't conflict here).

### Step 2 — Clean up tests / stories / MDX

After production code is clean, the test/story mocks become dead. Remove them in one batch commit per file (or group by component).

Commit message: `chore(tests+stories): remove unused window.mediaTools mocks`.

### Step 3 — Delete `packages/web/public/builder/` and `packages/web/public/vendor/`

```bash
git rm -r packages/web/public/builder packages/web/public/vendor
```

After deletion, verify `yarn build` still succeeds (catches any remaining HTML/CSS references).

Commit message: `chore: delete legacy public/builder and public/vendor bundles`.

### Step 4 — Delete loose legacy assets in `packages/web/public/`

For each file in the table above (jobs.css, sse-utils.js, progress-utils.js/css, command-labels.js, format-bandwidth.js + test, reload-on-restart.js, version-footer.js, index.html):

1. Grep the codebase for references to confirm nothing in React still imports/uses it:
   ```bash
   yarn dlx ripgrep "sse-utils|progress-utils|format-bandwidth|command-labels|reload-on-restart|version-footer|jobs\.css" packages/web/src
   ```
2. If a React equivalent doesn't exist for a file you can't simply delete (e.g., version-footer), port it or coordinate with the orchestrator. Most should be safe to delete.
3. Delete in batches:
   ```bash
   git rm packages/web/public/{sse-utils.js,progress-utils.js,progress-utils.css,format-bandwidth.js,format-bandwidth.test.ts,command-labels.js,reload-on-restart.js,version-footer.js,jobs.css,index.html}
   ```

After: `yarn build` must succeed; `yarn dev` must serve `/` and `/builder` without console errors.

Commit message: `chore: delete legacy public/ root assets (jobs page is now React-only)`.

### Step 5 — Delete `types.window.d.ts`

Once all `window.mediaTools` references are gone, delete the type declaration:

```bash
git rm packages/web/src/types.window.d.ts
```

Run `yarn typecheck` immediately — any stragglers W3 missed will surface as errors. Fix or restore the file.

Commit message: `chore: delete types.window.d.ts (window.mediaTools is gone)`.

### Step 6 — Collapse `createRoot` calls

Check `packages/web/src/app.tsx`. The legacy build mounted React in multiple roots (one per overlay modal). With legacy gone, there should be ONE `createRoot(document.getElementById("root"))`. If there are still multiple, collapse to one.

Commit message: `refactor(app): collapse to single createRoot call`.

### Step 7 — Audit any leftover Hono dev-proxy / index references

The `packages/server/src/api/hono-routes.ts` may have a dev-proxy that falls through to Vite, or a static handler for `public/`. Verify it doesn't try to serve the deleted files. If it does, update or remove those references.

Commit message (if needed): `refactor(server): drop static-file routes for deleted legacy assets`.

## Pre-push gate (Universal Rule #2 — applies to every commit)

```bash
yarn test run
yarn typecheck
yarn lint
```

All three must pass. **Do not skip `yarn test run`** — W2D got bit by this and shipped 7 failing tests that I had to fix later via the testing-library `afterEach(cleanup)` hook (commit 88802a6). That cleanup hook is now in place, so your tests should be reliable.

If lint auto-fixes things (Biome uses `--write`), commit the auto-fixes with the rest of your work — don't leave the working tree dirty between commits.

## Manual smoke after each major step

- `yarn dev` runs and serves `/` (jobs page) and `/builder` (sequence builder) without console errors.
- Build the production bundle: `yarn build` succeeds. Then `yarn workspace @media-tools/web run preview` (or whatever the preview command is) — load both routes again, verify no 404s.
- Open every modal in `/builder` (LoadModal, YamlModal, CommandHelpModal, ApiRunModal, PromptModal, LookupModal, FileExplorerModal) — they should render without errors.
- Drag-and-drop a step in the builder — works.
- DslRulesBuilder (in `modifySubtitleMetadata` step) — renders the visual builder, not the JSON fallback.

## Checklist updates (Universal Rule #8)

- At start: edit [docs/react-migration-checklist.md](../react-migration-checklist.md) — mark W3 🔄 In Progress with today's date and `claude-sonnet-4-6 (high effort)`.
- Per commit: append to Progress Log.
- At end: mark W3 ✅ Done with a one-line summary (e.g., "3 production calls replaced; 60+ legacy files deleted; types.window.d.ts gone; single createRoot").

## Forbidden (Universal Rule #4)

No `toMatchSnapshot`, `toMatchInlineSnapshot`, `toHaveScreenshot`. No Percy, Chromatic, or VRT tooling. Tests use inline expected values.

## When done

Reply with:
- Commit SHAs grouped by step (1: window.mediaTools replacements; 2: test/story cleanup; 3+: deletions; final: createRoot collapse)
- `yarn test run`, `yarn typecheck`, `yarn lint`, `yarn build` all green
- Manual smoke results (which modals you tested, any console output)
- File counts: how many files deleted total (`git diff --stat HEAD~N..HEAD | tail -1`)
- Any leftover `window.mediaTools` references (grep should show zero — if non-zero, list them)
- Any references to deleted assets that needed surprise updates (e.g., Hono routes)

The orchestrator will then generate W4 (parity verification + master merge) and W5 (e2e tests, parallel with W4).
