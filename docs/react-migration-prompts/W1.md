# W1 spawn prompt

Paste the block below into a fresh Claude Code session opened in `d:\Projects\Personal\media-tools`.

---

You are Worker W1 in the React Migration Recovery for media-tools.

**Working directory:** `d:\Projects\Personal\media-tools`
**Branch:** `react-migration` (already checked out — do not switch)
**Your model:** Sonnet 4.6, medium effort
**Your role:** Wave B-0 — the critical-path unlock. Port command definitions and `buildParams` from vanilla JS into TypeScript, replace the RenderFields placeholder with a real dispatcher, and ship the shared helpers W2A–W2D will consume.

## Required reading before doing anything

1. [docs/react-migration-recovery-handout.md](../react-migration-recovery-handout.md) — start with the **"Universal Rules — ALL WORKERS READ THIS"** section (all 8 rules apply to you), then find the **"WORKER W1 — Phase 1: Wave B-0 (Critical Path)"** section (your specific 8 tasks).
2. [docs/react-migration-checklist.md](../react-migration-checklist.md) — current state. Phase 0 (Pre-W0, W0a, W0b, W0c) is all done. You are unblocking W2A–W2D.

## Critical context from Phase 0 (changes the original plan)

**W0b's audit found:**

- `packages/web/src/components/FieldLabel/` is **already fully implemented** (`FieldLabel.tsx`, `.test.tsx`, `.stories.tsx`, `.mdx`). Your Step 4 (Port FieldLabel) should *verify* it matches the dispatcher's needs rather than recreating it. If it works, skip the step and note in the checklist.
- `packages/web/src/components/PathField/` **does not exist** — not your problem (it's W2D's), but if you happen to need `getLinkedValue` patterns from PathField, you'll have to read the legacy version directly.
- `EnumField/`, `LanguageCodeField/`, `NumberField/` exist as partials that reach into `window.enumPicker?.open(…)`, `window.setParam?.(…)`, etc. — not your problem; W2A/W2B/W2D will swap those out for Jotai dispatches when they extend each.
- `Wave E` commits may have been on a `worktree-wave-e` branch that didn't fully merge — flag this for W4, not for you.

**W0c shipped — read this carefully:**

- `packages/web/scripts/capture-parity-fixtures.ts` contains a **working, verified port of `buildParams`** (lines 68–147), labelled "ported verbatim from sequence-editor.js ~line 723". It produced 36 fixture pairs in `packages/web/tests/fixtures/parity/`. The fixtures are the empirical proof that this port is correct.
- **Strong recommendation:** lift `buildParams` from the capture script rather than re-porting from `sequence-editor.js`. The capture-script version has already been validated against every command. Re-porting risks introducing a divergence W4 will catch.
- Watch the signature: the capture script's `buildParams` takes `(step, commandDefinition)` — not `(step, paths, commands)` as the plan originally sketched. Pick whichever signature fits your dispatcher; both are valid. If you take a `Commands` map, the lookup is a thin wrapper.
- The capture script's `COMMANDS` import is still pointed at the legacy `../public/builder/js/commands.js` via dynamic import. After your Step 1 (port to TS), you can **optionally** update the capture script's import to `../src/commands/commands.ts` — this is technically W4's job (per W0c's commit message), but doing it now lets W4 just re-run the script rather than re-port.
- `packages/web/scripts/` is excluded from tsconfig — that script is one-off tooling, not part of the web build. If you do touch it, run `tsx` against it manually to verify (no `yarn typecheck` coverage).

## Your tasks (from the handout)

Execute the 8 steps in the W1 section. The expected commit shape is in the handout. Reminders:

- **Step 1 (commands.ts):** Port the full COMMANDS export. The legacy file is `packages/web/public/builder/js/commands.js`. Use the existing `Commands` type in `packages/web/src/types.ts`. The 36 fixture inputs use the legacy COMMANDS — your TS port must produce identical structure so the fixture inputs remain valid against your version.
- **Step 2 (buildParams.ts + tests):** Lift from `capture-parity-fixtures.ts` lines 68–147. Tests use **inline expected values** — pick representative commands (one per branch: simple, string link, object link, companionNameField, persistedKeys) and write `expect(...).toEqual({...})` against the actual fixture inputs.
- **Step 3 (links.ts, fieldVisibility.ts):** Port `getLinkedValue` from `sequence-editor.js` (it's a separate function from buildParams) and `field-visibility.js`. W2A–W2D import these.
- **Step 4 (FieldLabel):** Verify the existing implementation. If the dispatcher needs a different API, extend it — but coordinate with the existing tests so they still pass.
- **Step 5 (RenderFields dispatcher):** Each `case` block returns a `<TodoField type="<fieldType>" field={field} />` placeholder. W2A–W2D replace each placeholder with the real component. **Keep the dispatcher branches stable** — workers will edit one `case` block each via the `Edit` tool.
- **Step 6 (BuilderPage):** Drop the dynamic `import("/builder/js/commands.js")`. Use `useHydrateAtoms([[commandsAtom, COMMANDS]])`. Verify modals mounted via `createPortal(document.body)` still see the hydrated atom — they should if `Provider` is shared.
- **Step 7 (yamlSerializer):** Drop the `window.mediaTools?.buildParams` branch (line 13–22 of `packages/web/src/jobs/yamlSerializer.ts`). Use the TS `buildParams`. Add/update tests with inline expected values.
- **Step 8 (frozen fixture):** Create `packages/web/src/commands/__fixtures__/commands.ts` — a stable subset of commands for W2A–W2D test files. After this commit, do not modify.

## Pre-push gate (Universal Rule #2)

Before every push:

```bash
yarn test run
yarn typecheck
yarn lint
```

All three must pass. The infrastructure for this gate is reliable as of commit `1e7cf03` — if it fails, your change broke something.

## Commits (from the handout's W1 section)

1. `feat(commands): port COMMANDS to TS`
2. `feat(commands): port buildParams to TS with inline-assertion tests`
3. `feat(commands): port getLinkedValue and fieldVisibility shared helpers`
4. `feat(components): FieldLabel React component` (or `chore(components): verify existing FieldLabel meets dispatcher needs` if you skip recreation)
5. `feat(components): real RenderFields dispatcher with TodoField placeholders`
6. `refactor(builder): hydrate commandsAtom from TS commands module`
7. `refactor(jobs): yamlSerializer uses TS buildParams; drop window.mediaTools branch`
8. `test(commands): freeze __fixtures__/commands.ts for Phase 2 workers`

Optional bonus commit if you do the W0c follow-up:
- `chore(scripts): point capture-parity-fixtures at TS commands module`

Push each immediately after the pre-push gate passes. Before each push: `git pull --rebase origin react-migration`.

## Checklist updates (Universal Rule #8)

- **At start of session:** Edit [docs/react-migration-checklist.md](../react-migration-checklist.md) — mark W1 as 🔄 In Progress with today's date and your model.
- **After each commit:** Append one line to the Progress Log table.
- **At end of session:** Mark W1 as ✅ Done with the date and a one-line summary.

## Forbidden (Universal Rule #4)

No snapshot tests. No `toMatchSnapshot`, `toMatchInlineSnapshot`, `toHaveScreenshot`. No Percy, Chromatic, or VRT tooling. Spell expected values inline. See [AGENTS.md](../../AGENTS.md) "Forbidden test styles".

## When done

Reply with a short status update:
- 8 commit SHAs (or fewer if you combined / skipped Step 4)
- Whether you lifted `buildParams` from the capture script or re-ported (and why)
- Whether you updated the capture script's import (optional bonus)
- FieldLabel decision (verified existing vs rebuilt)
- Pre-push gate state for the final commit
- Any blockers or scope creep

The overseer (separate Claude session) will then prep W2A.md, W2B.md, W2C.md, W2D.md.
